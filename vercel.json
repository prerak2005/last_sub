{
    "builds":[
        {
            "src": "api/latency.py",
            "use": "@vercel/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/latency.py"
        }
    ]
}